#include <stdio.h>

/** main - start of program
 * Return: returns integer value 0
 * @alpha: used to hold alphabets
 */

int main()

{
	int a;

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	return (0);
}
