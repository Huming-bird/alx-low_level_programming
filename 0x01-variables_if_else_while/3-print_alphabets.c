#include <stdio.h>

/**
 * main - start of program
 * Return: returns integer dtype
 */

int main(void)
 
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	for (i = 'A'; i <= 'Z'; i++)
	{
		putchar(i);
	}
	return (0);
}
