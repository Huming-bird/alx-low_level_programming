
#include "main.h"
#include <stdio.h>

/**
 * _islower - start of program
 * @c: character
 *
 * Return: type int
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
