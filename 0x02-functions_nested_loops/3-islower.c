#include <stdio.h>
#include <stdlib.h>

/**
 * main - start of program
 * Return: type int
 */

int _islower(int c)
{
	int p;

	if (islower(p))
	{
		return (49);
	}
	else
	{
		return (48);
	}
	return (0);
}

int main(void)
{
	_islower(97);
	putchar('\n');
	return (0);
}

