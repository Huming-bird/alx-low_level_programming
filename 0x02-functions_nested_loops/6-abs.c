#include <stdio.h>
#include "math.h"

/**
 * _abs - start of prog
 * @int: int typ parameter
 *
 * Return: type int
 */

int _abs(int)
{
	int n;

	if (int == 0)
	{
		return (0);
	}
	else if (int < 0)
	{
		return (int * -1);
	}
	else
	{
		return (int);
	}

	return (0);
}
